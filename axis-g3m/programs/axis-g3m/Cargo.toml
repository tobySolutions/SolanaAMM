[package]
name = "axis-g3m"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.9"
pinocchio-system = "0.4"
pinocchio-token = "0.4"

[features]
no-entrypoint = []

[dev-dependencies]
